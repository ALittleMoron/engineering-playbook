# Git

- Do not create or use a Git worktree unless the user explicitly says that Git worktrees may be
  used.
- If a project has uncommitted changes, stop work and explicitly tell the user about them unless
  the user has already authorized working with those changes.
